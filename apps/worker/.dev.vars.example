# Copy to apps/worker/.dev.vars (gitignored) to point `wrangler dev` at the
# real Supabase project instead of the in-memory auth store.
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_SERVICE_ROLE_KEY=<service_role JWT from Supabase dashboard -> Project Settings -> API>
