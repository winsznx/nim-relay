# Rebuild deployment, 2026-09-17

Commit `5444734` on `production-relay`, deployed from a clean worktree.

| Network | URL | Worker version |
|---|---|---|
| MainAlbatross | https://nim-relay.timjosh507.workers.dev | `1ae8a709-c205-48b9-9f92-b9f4d9820d3e` |
| TestAlbatross | https://nim-relay-testnet.timjosh507.workers.dev | `9a72e7ea-c459-4e57-886e-d47edde43e98` |

## Verified before deploy

- Typecheck, lint and production build pass for the workspace.
- Package tests: 904 engine (including the 200-leg relay-leg golden corpus, also byte-identical in isolated workerd), 178 Worker, 44 relay protocol, 5 shared. Web unit tests: 212.
- Browser specs: network (17), leg, relay station, audio, ops.
- Two-runner lifecycle E2E (`apps/web/playwright.lifecycle.config.ts`): mocked Nimiq Pay provider in the page, mock Albatross JSON-RPC, isolated `wrangler dev` without the live database. All 7 scenarios passed on the integrated build; the main pass-there-and-back and decline scenarios re-ran on this commit.
- Mainnet's archived network state was migrated locally with the new normalizer before deploy; its one active baton kept its code, holder and status.

## Verified after deploy

- `/api/health` reports the expected network on both Workers; the public snapshot loads.
- Headless mobile browser on mainnet: world home with the migrated baton, a practice leg reaching the racing phase, the Relay Station and the proof page, with no runtime errors. A removed prototype path renders the product's not-found screen.

## Not verified

- A real native Nimiq Pay handoff between two physical phones on either network.
- Frame rate and thermal behaviour in the Nimiq Pay iOS WebView.
- Any real-user adoption.

## Found by the lifecycle E2E

The relay-protocol RPC client called `fetch` as a method of the client. The Workers runtime throws "Illegal invocation" for that call, so before this rebuild no deployed handoff could have verified. Fixed in `4d7e666` with a regression test.
